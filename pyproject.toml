[project]
name = "mariadb-server"
version = "0.2.4"
description = "MariaDB MCP Server"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "asyncmy>=0.2.10",
    "fastmcp[websockets]==2.12.1",
    "python-dotenv>=1.1.0",
]
